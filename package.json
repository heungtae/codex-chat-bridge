{
  "name": "@heungtae/codex-chat-bridge",
  "version": "0.1.9",
  "description": "Responses-to-chat/completions bridge for Codex workflows",
  "license": "Apache-2.0",
  "type": "commonjs",
  "bin": {
    "codex-chat-bridge": "bin/codex-chat-bridge.js"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "bin/",
    "scripts/",
    "src/",
    "Cargo.toml",
    "Cargo.lock",
    "README.md",
    "USAGE.md"
  ],
  "scripts": {
    "sync:version": "node scripts/sync-version-from-cargo.cjs",
    "build:bridge": "npm run sync:version && cargo build --release --bin codex-chat-bridge",
    "postinstall": "node scripts/postinstall-build.cjs",
    "start": "node bin/codex-chat-bridge.js",
    "prepack": "npm run sync:version",
    "pack:check": "npm pack --dry-run"
  },
  "dependencies": {
    "@heungtae/codex-chat-bridge": "^0.1.7"
  }
}
